// ─── Visualli Meta ─────────────────────────────────────────────────────────────
//
// VisualliMeta is an alias to the schema Meta type.
// It's imported from the auto-generated bindings to ensure sync with schema changes.

import type { Meta } from './schema.js';

/** Alias to schema Meta type for SDK consistency */
export type VisualliMeta = Meta;
